package com.fabio.GestionFacturas;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Smoke test: fails if the full Spring context can't be assembled (missing bean, bad config, etc). */
@SpringBootTest
class GestionFacturasApplicationTests {

	@Test
	void contextLoads() {
	}

}
